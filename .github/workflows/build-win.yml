name: Build Windows x64

on:
  push:
    branches:
      - main
  workflow_dispatch: # 支持在 GitHub 网页一键手动触发

jobs:
  build:
    name: Build Windows x64 (Electron 43.2.0)
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.13.1

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build Assets & Bundles
        run: pnpm run build:all

      - name: Build Windows x64 Portable Directory
        run: |
          pnpm exec electron-builder --dir --win --x64 --publish never

      - name: Pack Portable Content to Zip
        shell: pwsh
        run: |
          if (Test-Path "build/win-unpacked") {
            Compress-Archive -Path "build/win-unpacked/*" -DestinationPath "build/Open-Yami-RPG-Editor-win-x64.zip" -Force
            Write-Host "Portable zip created: build/Open-Yami-RPG-Editor-win-x64.zip"
          }

      - name: Upload Single Zip Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Open-Yami-RPG-Editor-win-x64
          path: build/Open-Yami-RPG-Editor-win-x64.zip
          retention-days: 30
